import { NvDirective } from './nv.directive';

describe('NvDirective', () => {
  it('should create an instance', () => {
    const directive = new NvDirective();
    expect(directive).toBeTruthy();
  });
});
