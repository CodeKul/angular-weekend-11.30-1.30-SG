import { AlertMakerDirective } from './alert-maker.directive';

describe('AlertMakerDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertMakerDirective();
    expect(directive).toBeTruthy();
  });
});
